> 과거 조사·전환 기록입니다. 2026-09-14부터 현재 코드는 KTX 전용이며 SRT 어댑터와 srtgo 의존성은 제거했습니다. 현재 사용법은 README.md를 참고하세요.

# KTX 인증 클라이언트 전환

검증일: 2026-09-12 (한국시간)

기존 `srtgo.ktx.Korail`은 올바른 저장 정보로도 로그인 응답 `MACRO ERROR`를 받았다.
KTX 실행·로그인 설정·예약 내역 확인 경로를 `korail_adapter.MobileKorail`로 통일했다.
기존 역/승객 설정 UI와 선택적인 SRT 어댑터는 `srtgo`를 계속 사용한다.

## 의존성과 동작

- `korail-mobile-api` 1.1.1, commit `e798fbcf08e56297b4d16a5c300bd7a4f167722d` 고정.
- 라이브러리의 `KorailConfig(enable_dynapath=True)` 사용. 라이브러리 소스는 수정하지 않는다.
- 자격 증명은 기존 환경변수/OS 키링에서 읽고 파일에 저장하지 않는다.
- 모든 httpx 요청에 프로젝트 HTTP 예산·타임아웃·리디렉션 제한을 적용한다.
- 시간대 전체를 페이지별로 조회한다. 실서버는 `h_next_pg_flg=Y`일 때도 커서 필드를
  생략하므로 마지막 출발 시각부터 다시 조회한다. 경계 시각을 포함하고 열차 키로 중복을 제거한다.
  시각/커서가 진행하지 않으면 중단한다.
- 중복 검사는 미결제 예약과 현재 승차권의 모든 페이지·승차권·구간을 포함한다.
  현재 승차권의 `h_row_cnt` 또는 다음 페이지 종료 표시로 완료를 판정하고,
  완료 정보가 없으면 빈 페이지까지 조회한다. 알 수 없는 형식이나 반복 페이지를 빈 목록으로 간주하지 않는다.
- 예약은 `--reserve`일 때만 `MutationConsent(allow_reserve=True, dry_run=False)`로 한 번 전송한다.
  좌석 우선순위는 전송 전에 정하고 실패 시 다른 좌석으로 재예약하지 않는다.
- 결제는 `--pay`를 함께 지정했을 때만 `MutationConsent(allow_payment=True, dry_run=False,
  fake_card_only=False, real_card_acknowledged=True)`로 한 번 전송한다. 취소·환불 권한은 열지 않는다.
  실패해도 재전송하지 않고 승차권 내역으로 결제 여부만 확인한다.
- 메뉴의 내역 확인은 조회 전용이며 취소·환불은 공식 앱으로 안내한다.

## 확인된 범위

- 저장된 KTX 계정으로 서비스 상태 확인·암호화 정보·로그인 총 HTTP 3회 성공.
- 열차 조회: HTTP 4회로 중복 제거 후 28편 조회.
  확인 시각의 일반실·특실 예약 가능 코드는 모두 `13`이었다.
- 미결제 예약과 현재 승차권 조회도 실서버에서 성공했다. 로그인 포함 HTTP 5회.
  승차권의 `reservation_list → ticket_list → train_info` 중첩 구조와 전체 건수 기반 종료를 확인했다.
- 실제 예약과 결제는 실행하지 않았다. HTTP 모의 전송 테스트로 인증 헤더,
  암호화 비밀번호, 페이지 경계, 승객/좌석 폼, 중복 검사, 예약 1회, 결제 1회와 결제 폼,
  결제 거절 후 승차권 내역 확인을 검증한다.
- 프로젝트 오프라인 테스트 63개 통과, `pip check` 의존성 충돌 없음.

구현자 문서: https://github.com/yakisoba0728/korail-mobile-api
